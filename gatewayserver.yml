server:
  port: 8072

# eureka:
#   instance:
#     hostname: localhost
#   client:
#     fetchRegistry: false
#     registerWithEureka: true # false
#     serviceUrl:
#       defaultZone: http://localhost:8070/eureka/

eureka:
  instance:
    preferIpAddress: true
  client:
    registerWithEureka: true
    fetchRegistry: true
    serviceUrl:
      defaultZone: "http://localhost:8070/eureka/"
